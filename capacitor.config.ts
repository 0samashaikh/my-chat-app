import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'my-chat-app',
  webDir: 'dist/my-chat-app',
  bundledWebRuntime: false
};

export default config;
